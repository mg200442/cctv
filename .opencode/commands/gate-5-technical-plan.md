---
description: Run Gate 5 technical plan and slice planning.
agent: orchestrator
---

Run Gate 5 using `.opencode/gates/gate-5-technical-plan.md`. Create small implementable slices under `.opencode/tasks/slices/` when useful and summarize them in `.opencode/project-status.md` so the user can approve the next steps from one file. For prepared designs, use many focused vertical slices when needed, and map each slice to the approved handoff, target technologies, fidelity criteria, and verification.

Arguments:

`$ARGUMENTS`
