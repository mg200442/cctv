---
description: Split approved work into small slices.
agent: orchestrator
---

Use `.opencode/skills/create-slice/SKILL.md`, create/update slice files in `.opencode/tasks/slices/` when useful, and update `.opencode/project-status.md` with each proposed or active slice plus the next approval-ready actions.

For prepared designs, create as many small vertical slices as needed to implement the approved handoff faithfully. Each slice must identify the relevant screen, state, component, breakpoint, asset, technology constraint, and design verification method.

Arguments:

`$ARGUMENTS`
