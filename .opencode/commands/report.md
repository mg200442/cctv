---
description: Create a report for a completed gate, slice, audit, or review.
agent: orchestrator
---

Use the `create-report` skill. Write the report under `.opencode/reports/YYYY-Www/`.

Arguments:

`$ARGUMENTS`
